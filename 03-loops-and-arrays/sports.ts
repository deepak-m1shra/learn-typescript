let sports: string[] = ['Cricket', 'Golf', 'Football', 'Kabaddi'];

for(let sport of sports) {
    if(sport === 'Kabaddi') {
        console.log(`My favourite sport is ${sport}`)
    } else {
        console.log(`Sport Name : ${sport}`)
    }
}